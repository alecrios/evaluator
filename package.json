{
  "name": "eval",
  "version": "0.3.0",
  "description": "Simple calculator app",
  "main": "app.js",
  "scripts": {
    "start": "electron .",
    "test": "mocha",
    "package-mac": "electron-packager . --overwrite --platform=darwin --arch=x64 --prune=true --out=release-builds --asar=false",
    "package-win": "electron-packager . --overwrite --platform=win32 --arch=ia32 --prune=true --out=release-builds --asar=true"
  },
  "author": "Alec Rios",
  "repository": {
    "type": "git",
    "url": "git://github.com/alecrios/eval.git"
  },
  "license": "MIT",
  "dependencies": {
    "electron": "^2.0.7"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "electron-packager": "^12.1.1",
    "mocha": "^5.2.0"
  }
}
